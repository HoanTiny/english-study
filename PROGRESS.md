# SpeakUp — Tiến độ dự án

> Cập nhật: 2026-06-04 · App học tiếng Anh A1 → giao tiếp (2h/ngày), mọi hoạt động đổ về 1 hub ôn tập SRS (FSRS), theo dõi khoảng cách **Hiểu → Nói được**.

## Stack
- Next.js 16.2.6 (App Router, Turbopack) · React 19 · TypeScript · Tailwind v4
- Supabase (Postgres + Auth ẩn danh + RLS)
- SRS thuật toán SM-2
- Web APIs: SpeechSynthesis (TTS), MediaRecorder
- AI: Google Gemini Flash (nhật ký + roleplay) · Azure Speech (chấm phát âm)

## Đã hoàn thành ✅

### Nền tảng & dữ liệu
- [x] Schema DB + RLS policies (`db/*.sql`)
- [x] Auth ẩn danh (Supabase) + AuthProvider
- [x] Migrate toàn bộ sang Supabase: Notes, Review (SRS), Journal, Shadowing
- [x] Dashboard "Hôm nay": thanh Hiểu→Nói được, thẻ đến hạn, streak nhật ký, điểm phát âm TB

### Core learning loop
- [x] Sổ tay (Notes) → đẩy cụm vào ôn tập
- [x] Hub Ôn tập SRS (SM-2): recognized vs mastered
- [x] Nhật ký 5–10 câu/ngày
- [x] Shadowing (nghe → nhại → chấm điểm)

### Nội dung khoá học (gốc, không copy giáo trình)
- [x] **Giai đoạn 1 — A1 Movers**: 11/11 bài
- [x] **Giai đoạn 2 — A2 Flyers**: 7/7 bài
- [x] **Giai đoạn 3 — B1**: 3/3 bài (restaurant, travel, opinions)
- [x] **Giai đoạn 4 — B1+**: think-in-english ✓ · ai-roleplay (trang riêng) ✓
- [x] Trang bài học `/lesson/[slug]` (cụm + câu mẫu + TTS + đẩy vào ôn tập)
- [x] Lộ trình cập nhật theo Cambridge "Pre A1 Starters/A1 Movers/A2 Flyers"

### Tính năng nâng cao
- [x] **Mở khoá bài học động** theo tiến độ SRS (chỉ bài đầu mở; bài kế mở khi bài trước được bắt đầu)
- [x] **Nhắc học hằng ngày** (Notification API, chọn giờ, banner trong app)
- [x] **Phản hồi nhật ký bằng Gemini** — `POST /api/journal-feedback` (fallback mock)
- [x] **Hội thoại AI roleplay** — `/roleplay` + `POST /api/roleplay` (5 tình huống, TTS)
- [x] **Chấm phát âm thật Azure** — `GET /api/speech-token` + Speech SDK ở shadowing (fallback mock)

## Đang chờ / cần làm 🔜

### Cấu hình API key (người dùng tự lấy — không tạo tài khoản hộ)
- [x] **Gemini**: `GEMINI_API_KEY` đã cấu hình trong `.env.local` → nhật ký AI + roleplay chạy thật
- [x] **Azure Speech (Free F0)**: `AZURE_SPEECH_KEY` + `AZURE_SPEECH_REGION=southeastasia` đã cấu hình → cấp token OK
- [x] Test thực tế 3 tính năng AI với key thật (2026-05-29): Azure token OK; Gemini 8/8 OK
  - Phát hiện Gemini Flash thỉnh thoảng trả 503 "high demand" → đã thêm **retry + backoff** (3 lần, 0.8s/1.6s) trong `src/lib/server/gemini.ts`

### Có thể làm thêm
- [x] Thêm link `/roleplay` ("Hội thoại") vào thanh Nav (sau Shadowing)
- [x] Tăng số cụm/câu cho các bài & shadowing (2026-05-29 → 30): tổng cụm **114 → 180**
  - [x] Shadowing: 5 → 16 câu (chia A1/A2/B1, khó tăng dần)
  - [x] Giai đoạn 1 (A1): 11 bài làm dày lên 7–9 cụm/bài
  - [x] Giai đoạn 2 (A2 Flyers): 7 bài làm dày lên 8 cụm/bài
  - [x] Giai đoạn 3–4 (B1/B1+): 4 bài làm dày lên 9 cụm/bài
- [x] **Gắn nhãn/kiểm tra cấp độ CEFR-J** (2026-05-30): dataset `data/cefrj-vocabulary-profile-1.5.csv` (7020 từ, license thương mại OK + ghi nguồn) + script QA `scripts/lint-cefr.mjs` (`npm run lint:cefr`) — dò từ vượt cấp trong câu mẫu, có xử lý hình thái từ (V-ing/-ed/-ly). Kết quả: chỉ còn từ vựng chủ đề tất yếu (đều có nghĩa Việt + ví dụ), không có lỗi nghiêm trọng.
- [x] **Luyện nghe YouTube theo cấp độ** (2026-05-30): mỗi bài có thẻ "🎧 Luyện nghe với người bản xứ" — kênh tự chọn theo CEFR (A1 Bob the Canadian · A2 VOA · B1 BBC · B1+ English with Lucy). Mặc định link tìm kiếm YouTube đã lọc kênh+chủ đề (hợp ToS, không chết link); có sẵn hạ tầng nhúng iframe `youtube-nocookie` qua trường tùy chọn `youtubeId`. (`ListeningResource.tsx`, `lib/listening.ts`)
- [x] **Phát âm thật từ Free Dictionary API** (2026-05-30): `GET /api/pronounce?word=` (proxy + cache trong RAM, TTL 7 ngày) trả IPA + audio mp3. Nút 🔊 ở bài học tự dùng audio người bản xứ cho từ đơn, fallback TTS cho cụm. Helper `src/lib/pronounce.ts`.
- [x] **Research nguồn tiếng Anh theo CEFR** (2026-05-30) → `docs/english-sources-research.md`
  - Stack an toàn: CEFR-J Wordlist (nhãn cấp độ, OK thương mại), Tatoeba (câu Anh–Việt, CC-BY), Free Dictionary API (IPA/audio), VOA (nghe, public domain), YouTube IFrame (nhúng video)
  - Tránh nhúng/copy: Oxford 3000/5000 & Cambridge EVP (chỉ tra cứu), British Council/BBC (chỉ link)
- [x] Nội dung sâu hơn cho Giai đoạn 3–4 (B1/B1+ lên 9 cụm/bài, ngôn ngữ hội thoại tự nhiên)
- [x] **Push notification thật (Web Push)** (2026-06-01): service worker `public/sw.js` + VAPID (`web-push`). Bật nhắc ở `/today` → subscribe + lưu `push_subscriptions` (Supabase, RLS) kèm giờ + tz_offset. Route `/api/push/test` (gửi thử cho chính mình, xác thực Bearer token) + `/api/push/send-due` (cron, bảo vệ `PUSH_CRON_SECRET`, gửi đúng giờ local + chống trùng `last_sent`). `StudyReminder` có nút "🔔 Gửi thử". `next build` sạch (34 route). ⚠️ Cần: chạy `db/migrate_push_subscriptions.sql` + (khi deploy) cron gọi `/api/push/send-due?secret=...` vài lần/giờ.

### Áp dụng design Figma (2026-05-30)
> Thiết kế: file Figma "Untitled" (P8ts69z7…), kết nối qua Figma MCP. Hướng: re-skin theo design (teal/sáng/phẳng), pha chút liquid-glass.
- [x] **Design tokens**: bảng màu teal (`#2B788B` primary, `#C3DCE3` soft, `#945069/#F2D4DC` pink, nền `#F6F5F4`, viền `#E0E0E0`, chữ phụ `#757575`) cho cả light + dark (dark retune teal). Biến mới: `--primary-soft`, `--pink`, `--pink-soft`.
- [x] **Fonts**: thêm **Dela Gothic One** (heading, class `.font-display`) + **Montserrat** (body) — Google Fonts mở, hỗ trợ tiếng Việt.
- [x] **Class dùng chung** chuyển từ liquid-glass → phẳng mềm (cascade ra mọi trang): `liquid-glass-card` (thẻ trắng viền nhạt + bóng mềm), `liquid-glass-btn` (pill teal đặc), `liquid-nav` (thanh phẳng), tắt blob nền, `text-gradient-iridescent` → teal đặc.
- [x] **Nav**: thanh phẳng top-bar (logo Dela Gothic teal + active teal-soft), giữ hamburger mobile.
- [x] **Trang chủ `/`**: layout theo design (hero eyebrow + heading display + CTA teal + stats 180+/22+, 3 section xen kẽ mini-game/từ vựng/tiến độ), giữ showcase game + roadmap.
- [x] Đã xem trực tiếp trên Chrome (light + dark) — đồng nhất. `next build` sạch (15 route).
- [ ] (Tùy chọn) Bộ minh hoạ 3D bản quyền hợp lệ để thay emoji placeholder; restyle sâu các trang con nếu cần.
- Trang `/preview` giữ làm bản đối chiếu (có thể xoá sau).

#### Triển khai 11 màn Figma (đợt 2) — 2026-05-30
- [x] **Sprint game**: chỉnh màu theo design (Right=teal-soft, Wrong=pink-soft, bỏ gradient indigo); cấu trúc play+results vốn đã khớp design (timer tròn, x mult/điểm, "I know/I don't know").
- [x] **Trang Thống kê** `/statistics` (mới): tabs Hôm nay/Toàn thời gian (underline teal), số liệu THẬT từ `loadDashboard` (Ôn tập SRS + Shadowing), **biểu đồ recharts** điểm phát âm theo câu, empty state khi chưa có dữ liệu. Thêm link "Thống kê" vào Nav. Dep mới: `recharts`.
- [x] **Audio-call game** `/audio-call` (mới, build lại đúng design 6:946/1017/1054): game NGHE **trắc nghiệm + mạng** — nghe TTS → chọn nghĩa đúng trong 5 lựa chọn; 5 tim, chọn sai/"I don't know" mất 1 tim; sau khi chọn hiện ảnh (emoji) + nhãn từ + đáp án đúng (teal)/sai (pink) + Next; phím 1-5/Space/Enter; hết tim → kết quả (mạng còn + từ đã nghe + I know/I don't know). Link từ section mini-game trang chủ.
- [x] **Sprint welcome**: vòng cấp độ tô màu theo design (A1 lá · A2 vàng · B1 đỏ · B2 đỏ đậm · C1 tím · C2 teal).
- [x] `next build` sạch — 17 route.
- Lưu ý: 6 link Figma trỏ nhầm vào lớp Nav (không phải màn riêng) → chưa rõ có màn Audio-call/Classbook/About bản đầy đủ; Classbook (feed xã hội) khuyên cân nhắc vì lệch định vị app.

### Tính năng Từ vựng (Active Recall) — 2026-05-30
> Dựa trên doc người dùng cung cấp (Lexical Approach + Active Recall + Contextual Flashcard). Tách thành tính năng riêng.
- [x] **`/vocab` nâng thành THƯ VIỆN bộ thẻ** (2026-05-30, tham khảo Parroto): gom theo collection (Khởi đầu A1 / Mở rộng A2 / Giao tiếp B1+), mỗi bài = 1 bộ thẻ (bìa gradient + emoji + badge CEFR + số thẻ). Click bộ → vào phiên học. **Khác/hơn Parroto**: tất cả miễn phí (không PRO khóa), mỗi thẻ học bằng Active Recall + audio bản xứ + câu ví dụ AI (✨).
- [x] **Import từ vựng theo chủ đề + học theo TỪ ĐƠN** (2026-05-30):
  - (Lần 1, PDF) — bỏ: PDF lỗi font, đã thay bằng nguồn Excel sạch hơn.
  - (Lần 2, **Excel** — dùng) 21 file `.xlsx` người dùng gửi (mỗi file = 1 chủ đề). Đọc bằng SheetJS → **431 từ, 395 có IPA chuẩn + nghĩa Việt sạch** (kể cả mẫu câu chào hỏi). Script `scripts/import-xlsx.mjs` → `src/data/vocab.json`.
  - `/vocab` thêm collection **"Từ vựng theo chủ đề"** (21 chủ đề: Con vật nuôi, Cảm xúc, Máy tính & Internet, Trường học…; badge "Từ đơn" + số từ + IPA). Học **theo từ đơn** (không chỉ cụm) — cả 3 chế độ Flashcard/Đoán/Trắc nghiệm; guard khi thẻ không có câu ví dụ. Audio từ đơn = phát âm bản xứ (Free Dictionary). `next build` sạch (20 route).
- [x] **Nâng UI học từ vựng + nối Sổ tay/Ôn tập** (2026-05-30, theo ảnh Parroto): Flashcard reveal có **4 nút FSRS** (Học lại/Khó/Tốt/Dễ kèm nhãn thời gian) → bấm = lưu từ vào **Sổ tay (notes)** + đẩy vào **Ôn tập FSRS** với điểm tương ứng. Thêm **✓ Thành thạo** (đã thuộc, bỏ qua, không ôn) và **🔖 Lưu vào sổ tay** (lưu để xem lại). Chế độ Đoán/Trắc nghiệm cũng có nút "Lưu vào sổ tay" (từ sai → lịch ôn sớm). Có toast xác nhận. Đã kiểm chứng: từ lưu từ vocab xuất hiện ở `/review` với thanh Sức mạnh trí nhớ + 4 nút chấm.
- [x] **Học theo từng từ ĐA CHẾ ĐỘ** (2026-05-30, theo Parroto): tab chuyển 3 chế độ trong phiên học — **Flashcard** (lật, tự chấm), **Đoán/gõ** (gõ cụm + ô trống trong ví dụ + Gợi ý + kiểm tra), **Trắc nghiệm** (nghĩa → chọn 1/4 cụm, phản hồi đúng/sai màu). Dùng chung tiến độ + audio + (Flashcard) câu ví dụ AI. Đã xem 3 tab + Flashcard + Trắc nghiệm trên Chrome.
- [x] **Trang `/vocab` "Từ vựng"** (luồng học): luyện **nhớ chủ động** — hiện nghĩa Việt + nghe phát âm (audio người bản xứ cho từ đơn, fallback TTS) → người học tự bật ra cụm → "Hiện đáp án" lật xem cụm + IPA + câu ví dụ (ngữ cảnh) → tự chấm "Nhớ được / Chưa nhớ". Phiên 20 cụm, lọc cấp độ (Tất cả/A1/A2/B1+), nguồn = 180 cụm trong `lessonContent`, màn kết quả. Thêm "Từ vựng" vào Nav.
- [x] **Game Ghép cụm `/collocations`** (mới, theo Lexical Approach): ghép động từ ↔ tân ngữ đi kèm (make→a decision, take→a break…); mỗi vòng 5 động từ khác nhau, ghép đúng khóa teal + đọc cụm + hiện nghĩa, điểm số, vòng tiếp theo. Link từ trang Từ vựng. Không dùng cơ chế "tim" (theo khuyến nghị doc).
- [x] **Gemini sinh câu ví dụ đa dạng**: `POST /api/example-sentence` ({en,vi} → câu tiếng Anh mới + dịch, fallback khi chưa cấu hình key). Nút "✨ Câu ví dụ khác (AI)" ở mặt sau thẻ Từ vựng → tạo câu ngữ cảnh mới + đọc audio (chống học vẹt, theo doc). Đã test với key thật.
- [x] **Game Ghép cụm `/collocations`** (xem trên).
- [x] `next build` sạch — 20 route.
- [x] **Nâng SM-2 → FSRS** (2026-05-30): `src/lib/srs.ts` viết lại dùng **ts-fsrs** (mô hình difficulty/stability/retrievability). **Không migration DB** — tái dùng cột cũ: `ease_factor`↔difficulty, `interval_days`↔stability(làm tròn), `repetitions`↔reps, `due_date`. `reviewRepo`/UID giữ nguyên (4 nút Lại/Khó/Tốt/Dễ). "mastered" = stability ≥ 21 ngày. Thêm hàm `memoryStrength()` + thanh **"🧠 Sức mạnh trí nhớ"** trên thẻ ôn tập.
  - Kiểm chứng: node test (good→2→11→46→163d, again→2d, easy→8d) + e2e trên Chrome (đẩy thẻ → ôn → chấm Tốt → bar đổi "Thẻ mới"→"21%·~2d", lưu Supabase). `next build` sạch (20 route).
  - Lưu ý còn lại: doc phê phán cơ chế "tim/mạng" — cân nhắc ở Audio-call.

### Ngữ pháp (cấu trúc câu) + Mẫu câu giao tiếp — 2026-05-31
> Nguồn: 7 file Excel câu giao tiếp người dùng gửi (Restaurant, thời tiết, lời khen, du lịch, câu giao tiếp mọi tình huống…). 434 câu.
- [x] **Tính năng Grammar `/grammar`** (mới): dùng Gemini phân tích 434 câu → rút **28 cấu trúc câu** chuẩn (khung dạng `I'd like to [verb]`, `It's [adj] to [verb]`…) + giải thích cách dùng (VN) + ví dụ + cấp độ A1/A2/B1. Script `scripts/extract-grammar.mjs` → `src/data/grammar.json`. Trang có lọc cấp độ + audio ví dụ. Thêm "Ngữ pháp" vào Nav.
- [x] **Mẫu câu giao tiếp** trong `/vocab`: 434 câu (19 nhóm tình huống) thành collection deck "Câu" — học bằng Flashcard/Trắc nghiệm. `src/data/sentences.json` (script `import-xlsx.mjs` mở rộng đọc mọi sheet).
- [x] **Chế độ Luyện tập Grammar** (2026-05-31): tab "✍️ Luyện tập" trong `/grammar` — mỗi cấu trúc, người học **tự đặt câu** → `POST /api/grammar-check` (Gemini) chấm: ✓ Tốt lắm / ✗ Cần chỉnh + câu sửa gợi ý (có audio). Đã test e2e: câu đúng → khen, câu sai → sửa.
- [x] `next build` sạch — 22 route. Đã xem `/grammar` (tra cứu + luyện tập) trên Chrome.

### Bổ sung 3 tính năng (đối chiếu yêu cầu) — 2026-05-31
- [x] **Accent UK & US + thu âm nghe lại**: `/api/pronounce` tách `us`/`uk` (audio + IPA) từ Free Dictionary. Component `PronounceBar` (nút 🔊 UK / 🔊 US đối chiếu + 🎙️ Thu âm / ▶️ Nghe lại bằng MediaRecorder) gắn vào reveal Flashcard ở `/vocab`. (Đã test API: "water" → US /ˈwɔtəɹ/ + UK /ˈwɔːtə/.)
- [x] **Module "3 thì cơ bản"** trong `/grammar` (tab 🕐): Simple Present / Present Continuous / Simple Past — bảng công thức (KĐ/PĐ/NV), khi nào dùng, dấu hiệu, ví dụ có audio. Nội dung GỐC (`src/data/tenses.ts`), không copy giáo trình. Link sang Luyện tập đặt câu.
- [x] **Cambridge Dictionary**: không nhúng trực tiếp được (không có API miễn phí; scrape vi phạm ToS; iframe bị chặn). Giải pháp hợp lệ: nút **"📖 Tra trên Cambridge ↗"** trong `PronounceBar` → mở trang Cambridge Anh–Việt cho từ đó (tab mới). Audio/IPA trong app vẫn dùng Free Dictionary.
- [x] Phần "danh sách Cambridge/British Council": KHÔNG import (bản quyền) — dùng nguồn hợp lệ sẵn có + tính năng tra nghĩa/ví dụ/audio đã đủ chức năng tương đương.
- [x] `next build` sạch — 22 route. Đã xem `/grammar` (3 thì) trên Chrome.

### Luyện nghe theo chủ đề (2026-05-31, kiểu Parroto)
- [x] **Trang `/listening`** (mới, vào Nav "Luyện nghe"): chip chủ đề (Hội thoại hằng ngày · BBC 6 Minute English · Phát âm & IPA) → grid video (thumbnail i.ytimg + level + kênh) → click mở **trình phát YouTube nhúng** (`youtube-nocookie`, hợp lệ ToS) + hướng dẫn luyện (không phụ đề → có phụ đề → shadowing) + link sang Shadowing chấm điểm.
- [x] Nguồn UY TÍN, MIỄN PHÍ tìm qua WebSearch. **Mở rộng 7 chủ đề** (`src/data/listenVideos.ts`, ~30 video): Hội thoại hằng ngày (VOA), Du lịch, Truyện cho trẻ em (Fairy Tales), Phát âm & IPA (Rachel's English), BBC 6 Minute English, TED-Ed, Luyện thi IELTS. Mỗi video gắn cấp độ A1/A2/B1/B2.
- [x] **Bộ lọc cấp độ** (Tất cả/A1/A2/B1/B2) trên trang Luyện nghe — lọc video trong chủ đề đang chọn. Đã test: chọn A2 → chỉ còn video A2.
- [x] **Bài tập nghe – Chép chính tả** `/dictation` (2026-05-31): nghe audio (TTS) câu giao tiếp → gõ lại → chấm (khớp 100% hoặc ≥90% từ = đúng, ≥50% = "gần đúng") + hiện đáp án + nghĩa Việt. Gamify: **điểm + chuỗi đúng 🔥 + tiến độ + màn kết quả** (đúng/chuỗi tốt nhất/điểm). Chọn chủ đề (19 nhóm câu) hoặc tất cả; phiên 10 câu; có nút Nghe lại / Gợi ý / Bỏ qua. Link nổi bật từ trang Luyện nghe. Đã test e2e trên Chrome.
- [x] **Chép chính tả – nâng cấp kiểu Parroto** `/dictation` (2026-05-31): bố cục 3 vùng (nghe | gõ | Bản chép). **Ô che TỪNG TỪ**, bấm 👁 lộ riêng từng từ (lộ thì không tính điểm); độ khó **Easy/Normal/Hard** (hiện chữ đầu / che theo độ dài / giấu cả độ dài); cột **Bản chép** liệt kê các đoạn, click nhảy đoạn + **thanh tiến độ %**; chấm tô xanh (đúng)/hồng (sai). 2 NGUỒN: **(a) Kho câu (TTS)** câu của app — hợp lệ 100%; **(b) Video YouTube** — dán link video có phụ đề → route `/api/yt-transcript` (lib `youtube-transcript`, endpoint timedtext không chính thức, học cá nhân) lấy transcript, đồng bộ phát đúng đoạn qua **YouTube IFrame Player API** (`YtPlayer.tsx`, seek + dừng sau N giây).
- [x] **Chép chính tả – chia câu chính xác bằng srv3 + Gemini** (2026-05-31): route `/api/yt-transcript` lấy phụ đề định dạng **srv3** qua **InnerTube API** → có **mốc thời gian TỪNG TỪ** → dừng đúng ranh giới (không trôi sang câu sau). Phụ đề ASR không dấu câu → **Gemini chấm câu lại** (giữ nguyên từ, chỉ thêm dấu câu/viết hoa) rồi map về timestamp → tách đúng câu. Có cache theo video + fallback nhiều lớp (srv3 → cue; gemini → heuristic). `YtPlayer` dừng theo vị trí phát thực tế (`getCurrentTime`).
- [x] **Lưu video YouTube đã dùng** (2026-05-31): bảng `dictation_videos` (RLS theo user) + `dictationVideosRepo.ts`. Mỗi lần dán link & tải transcript thành công → tự lưu (id/tiêu đề/kênh qua oEmbed). Trang `/dictation` (chế độ YouTube) hiện danh sách **Video đã lưu** (thumbnail + tiêu đề + kênh) → bấm để mở lại không cần dán link, có nút xoá. ⚠️ Cần chạy `db/migrate_dictation_videos.sql` trên Supabase.
- [x] **Nhãn "có phụ đề" + gợi ý video** (2026-05-31): script `scripts/check-captions.mjs` kiểm tra 30 video trong `listenVideos.ts` qua InnerTube → gắn cờ `cc` (27/30 có phụ đề). Trang Luyện nghe: badge "📝 Có phụ đề" trên thẻ video; nút "Chép chính tả video này" tự khoá nếu video không có phụ đề. Trang `/dictation` (YouTube): thêm mục "📝 Gợi ý từ Luyện nghe · có phụ đề" — bấm là chép chính tả ngay, không cần dán link.
- [x] **CMS quản lý video Luyện nghe** (2026-05-31): bảng `listen_videos` (public read, ghi qua service-role). Trang `/admin/listening` (khoá bằng `ADMIN_PASSCODE`): thêm video qua link (tự lấy tiêu đề/kênh + kiểm tra CC), sửa tiêu đề/chủ đề/cấp độ, ẩn/hiện, sắp xếp (▲▼), xoá, và "Kiểm tra phụ đề hàng loạt". Route `/api/admin/listen-videos` (GET/POST/PATCH/DELETE) dùng `supabaseAdmin` (service role) + `checkAdmin` (header x-admin-key). Trang Luyện nghe đọc từ DB (`listenVideosRepo`) — fallback seed tĩnh nếu DB trống. Helpers `lib/server/youtube.ts` (parseVideoId/hasCaptions/getMeta). Script `scripts/seed-listen-videos.mjs` đổ 30 video sẵn. ⚠️ Cần: chạy `db/migrate_listen_videos.sql`, thêm env `SUPABASE_SERVICE_ROLE_KEY` + `ADMIN_PASSCODE`, rồi `node scripts/seed-listen-videos.mjs`.
- [x] **Google OAuth đã bật & test OK** (2026-06-01): tạo/đính callback `https://qsmazqhmwfohaauimvkv.supabase.co/auth/v1/callback` vào OAuth client Google + bật provider Google trong Supabase → đăng nhập Google thành công.
- [x] **Đăng nhập tài khoản thật + streak** (2026-05-31): `auth.tsx` viết lại — vẫn ẩn danh mặc định (app chạy ngay), thêm Email+mật khẩu & **Google OAuth** (`signUpEmail/signInEmail/signInGoogle/signOut`). `supabase.ts` bật `detectSessionInUrl`+PKCE cho OAuth. `profileRepo.ts`: `ensureProfile`/`touchStreak` (đếm streak theo ngày local)/`getProfile`/`updateDisplayName`. Trang `/login` (tab Đăng nhập/Đăng ký + nút Google), `/account` (avatar, 🔥streak, giai đoạn, đổi tên, đăng xuất). Nav: nút "Đăng nhập" khi ẩn danh / chip 🔥streak+avatar khi đã đăng nhập. ⚠️ Cần bật provider Google trong Supabase (+ Google Cloud OAuth redirect `https://<ref>.supabase.co/auth/v1/callback`) và (tuỳ chọn) tắt "Confirm email" để đăng nhập tức thì.
- [x] **Onboarding chọn trình độ (sau đăng ký)** (2026-05-31): cột `profiles.onboarded`; `/onboarding` hiện sau khi tạo tài khoản — **tự chọn nhanh 4 mức** (A1–B2 → current_stage 1–4) HOẶC **bài test xếp loại 8 câu** (`src/data/placement.ts`, đếm đúng → đề xuất mức). `setOnboarding`/`isOnboarded` trong profileRepo. Đăng ký email & Google đều redirect về `/onboarding`; trang tự chuyển `/today` nếu đã onboard, chuyển `/login` nếu khách ẩn danh. ⚠️ Cần chạy `db/migrate_onboarding.sql`.
- [x] **Cổng onboarding toàn cục** (2026-05-31): `OnboardingGate` (mount ở layout) — user thật chưa `onboarded` → tự đưa vào `/onboarding` từ mọi trang (bỏ qua /onboarding, /login, /admin), nên kể cả khi bật "Confirm email" (đăng ký → xác nhận email → đăng nhập) vẫn vào đúng onboarding. `auth.tsx` thêm `onboarded`/`profileReady`/`markOnboarded`; onboarding chờ `profileReady` để tránh đua trạng thái (trước đây bị đá nhầm về /login ngay sau đăng ký).
- [x] **Gộp dashboard "Hôm nay" vào trang hồ sơ** (2026-05-31): tách `TodayDashboard` (tỉ lệ Hiểu→Nói, 3 nhiệm vụ Ôn tập/Nhật ký/Shadowing, nhắc học) dùng chung; `/today` và `/account` đều render. `/account` giờ là hub cá nhân (hồ sơ + streak + giai đoạn + đổi tên + đăng xuất + tiến độ hôm nay).
- [x] **Game Sprint thành trang riêng + 3 nguồn từ** (2026-05-31): trang `/sprint` (link từ trang chủ & menu Kỹ năng). `SprintGame` viết lại theo pool-driven, chọn nguồn từ: **Cấp độ CEFR** (kho chuẩn), **Chủ đề** (431 từ trong vocab.json theo topic), **🤖 AI tạo mới** (route `/api/sprint-words` — Gemini sinh từ theo level + chủ đề tuỳ chọn). Trang chủ đổi tab Sprint thành CTA "Mở trang Sprint →".
- [x] **Sprint → lưu từ "Chưa thuộc" vào Ôn tập** (2026-05-31): màn kết quả Sprint có nút "🔖 Lưu tất cả vào Ôn tập" + lưu lẻ từng từ → `addNote`(inReview) + `gradeNote("again")` (đẩy vào FSRS, nghĩa lưu ở field example, tag = nguồn bộ từ). Nối game với pipeline ôn tập sẵn có.
- [x] **Sổ tay: thêm ô NGHĨA cho từ/cụm** (2026-05-31): cột `notes.meaning` + ô nhập "Nghĩa" trong form, hiển thị nghĩa (màu primary) ngay dưới từ trong thẻ. `addNote`/`listNotes` có **fallback** nếu cột chưa migrate (không vỡ trang). Tự truyền nghĩa khi lưu từ ở Sprint / Từ vựng / Từ điển. ⚠️ Chạy `db/migrate_note_meaning.sql` để bật.
- [x] **Từ điển resilient + loại từ/nhiều nghĩa** (2026-05-31): thẻ từ vựng Sổ tay có nút 🔊 phát âm (`PronounceMini`) + "📖 Loại từ & nghĩa khác" (`WordDetail` tra `/api/dict`, hiện pos + tối đa 3 nghĩa). Sửa `/api/dict`: khi Gemini lỗi/hết quota (429) → **fallback Free Dictionary** (loại từ + định nghĩa Anh + audio) thay vì báo "không tìm thấy"; phân biệt source `gemini`/`freedict`/`overloaded`/`notfound`. Modal báo rõ khi quá tải. (Model `gemini-3-flash-preview` free-tier rất thấp → nên đổi GEMINI_MODEL sang bản quota cao hơn.)
- [x] **Loại từ cạnh từ + Họ từ** (2026-05-31): `/api/pronounce` trả thêm `pos` (n/v/adj…) từ Free Dictionary → `PronounceMini` tự tải khi hiện thẻ, hiển thị **chip loại từ + IPA ngay cạnh từ**. `/api/dict` trả thêm `family` (Gemini: word family, vd. adventure→adventure(v)/adventurer(n)/adventurous(adj)/adventurously(adv)) → `WordDetail` hiện mục "🧩 Họ từ liên quan".
- [x] **Minh hoạ 3D thay emoji** (2026-06-01): component `Icon3D` dùng **Microsoft Fluent Emoji 3D** (MIT) qua CDN jsdelivr + **fallback emoji** nếu ảnh lỗi. Áp dụng trang **Hôm nay** (stats grid, 3 task block, streak, empty state); map ~25 emoji đã verify 200. (Còn lại: rollout Nav/Sprint/Vocab/Notes/Listening nếu muốn.)
- [x] **Nâng cấp học IPA `/ipa`** (2026-06-01): research cách học hiệu quả (minimal pairs + **HVPT** perception-first) → trang tương tác 3 chế độ: **(1) Bảng 44 âm** theo nhóm (phụ âm/nguyên âm đơn/đôi), click nghe + khẩu hình + từ ví dụ + đánh dấu âm người Việt hay sai; **(2) Nghe & nhại** (lọc theo nhóm/âm hay sai, thu âm MediaRecorder nghe lại); **(3) Phân biệt cặp âm HVPT** (forced-choice "nghe từ nào?" + feedback tức thì + điểm/chuỗi). Dữ liệu `src/data/ipa.ts` (44 âm + 12 minimal pairs). Audio người bản xứ (Free Dictionary) fallback TTS. Vào Nav (Kỹ năng) + CTA từ bài học `ipa-sounds`.
- [x] **IPA: bộ thẻ trượt + thu âm + câu ví dụ cache DB** (2026-06-01): modal âm có **bộ thẻ xếp chồng (WordCardDeck)** — mỗi âm 4 từ, mỗi thẻ có IPA + nút Nghe + **vuốt chuột/cảm ứng** chuyển thẻ + **nút thu âm nhại lại** (MediaRecorder). **Câu ví dụ riêng từng từ**, cache ở **bảng DB `word_examples`** (route `/api/word-example`: đọc DB → sinh Gemini/Free Dictionary → lưu); script `scripts/seed-word-examples.mjs` đã seed 141 từ. ⚠️ Cần `db/migrate_word_examples.sql`.
- [x] **Sửa modal "bị nhốt" trong layout + đồng bộ phát âm** (2026-06-01): layout mới (ClientShell) có transform → `position:fixed` bị giới hạn trong cột giữa (backdrop lệch, không căn giữa, vẫn cuộn nền). Sửa bằng **React Portal ra `document.body` + khoá cuộn + nền đặc** cho modal IPA và **DictionaryModal**. Thêm `PronounceMini` (🔊 + loại từ + IPA) vào trang **Ôn tập**.
- [x] **Rollout Icon3D ra sidebar + chip loại từ ở Từ vựng** (2026-06-01): toàn bộ icon menu Nav (Lộ trình/Hôm nay/IPA/Sprint/Hội thoại/…) dùng **Fluent Emoji 3D** (thêm 🧭📞📕🎒🔤💬🎮… vào map). Trang **Từ vựng**: thẻ từ đơn hiện **chip loại từ** (danh từ/động từ… từ vocab.json) cạnh IPA ở cả mặt lật & màn kết quả trắc nghiệm.
- [x] `next build` sạch — 33 route.

### Từ điển tra cứu trong app (2026-05-31, kiểu Parroto, nguồn FREE)
- [x] **`/api/dict?q=`**: Gemini cho nghĩa Việt + định nghĩa + ví dụ (tra cả **Anh→Việt và Việt→Anh**, tối đa 3 mục) + Free Dictionary cho **audio/IPA US-UK** thật. Không cần Cambridge (không có API free).
- [x] **`DictionaryModal`** + nút **"🔎 Tra từ"** trên Nav (mở popup tra). Mỗi kết quả: từ + loại từ + 🔊 UK/US + nghĩa Việt + định nghĩa EN + ví dụ EN/VI + **"+ Lưu"** (vào Sổ tay + Ôn tập FSRS). Đã test e2e: "xin chào"→hello/hi; "improve"→3 nghĩa, có audio.
- [x] **Bôi đen → tra nhanh** (2026-05-31): `SelectionLookup` (gắn ở layout, toàn app) — chọn 1 từ/cụm bất kỳ → hiện nút "🔎 Tra …" nổi cạnh chỗ chọn → mở `DictionaryModal` (nhận `initialQuery`, tự tra). Bỏ qua khi chọn trong ô nhập. Đã test e2e: double-click "meet" → tra ra 3 nghĩa + audio UK/US.
- [x] `next build` sạch — 23 route.

### Sửa UX trang Từ vựng (2026-05-31)
- [x] `/vocab` trước đây 1 trang cuộn dài (~62 bộ thẻ) → khó tìm. Nay có **3 tab danh mục lớn** ở đầu (📗 Theo bài học · 🔤 Từ theo chủ đề · 💬 Mẫu câu giao tiếp) kèm số bộ; chỉ hiện danh mục đang chọn. Thêm **ô tìm kiếm** lọc bộ thẻ theo tên. Component `DeckGrid` dùng chung. Đã xem trên Chrome — chuyển tab + lọc hoạt động.

### Polish giao diện (2026-05-30)
- [x] Sửa lỗi `animate-fadeIn` (dùng 17 chỗ nhưng chưa định nghĩa → hiệu ứng xuất hiện trang không chạy) + `scale-102` chưa định nghĩa
- [x] Thêm `prefers-reduced-motion` (tắt blob/animation cho người nhạy chuyển động), focus-ring bàn phím, scrollbar mảnh hợp tông, smooth scroll, màu bôi chọn thương hiệu
- [x] Thêm 2 nút CTA ở hero trang chủ ("Bắt đầu học hôm nay" → /today, "Học thử bài đầu tiên" → /lesson/greetings)
- [x] **Sửa Nav responsive cho mobile**: trước đây menu rộng ~811px tràn ngang ở màn hình <768px. Nay <md hiện nút hamburger + panel lưới 2 cột; ≥md giữ menu ngang. Đã soi thật trên Chrome (390px) — chuẩn. Đóng menu tự động khi chuyển trang.
- [x] Đã kiểm tra mobile các component trang chủ (Sprint/CoreLoop) — đã responsive sẵn (`flex-col md:flex-row`), không cần sửa.
- [x] `next build` sạch — 14 route

### Mở rộng nội dung bài học (2026-06-01)
- [x] **+9 bài chủ đề mới** (nội dung gốc, 8 cụm/bài) → tổng **31 bài / 253 cụm** (trước: 22 bài / ~180 cụm). Gắn vào curriculum + chuỗi mở khoá động:
  - A1: `clothes-appearance` (Quần áo & ngoại hình), `house-home` (Nhà cửa & đồ đạc)
  - A2: `shopping-money` (Mua sắm & trả giá), `feelings-emotions` (Cảm xúc), `making-plans` (Hẹn hò & rủ rê)
  - B1: `technology-phone` (Công nghệ & điện thoại), `work-office` (Công việc), `environment-nature` (Môi trường)
  - B1+: `small-talk` (Nói chuyện xã giao)
- [x] **Cải thiện trải nghiệm học** (2026-06-01):
  - **Quiz cuối bài** (`LessonQuiz.tsx`): trắc nghiệm nghĩa→chọn cụm đúng, chấm điểm, đọc đáp án (TTS), màn kết quả + làm lại. Gắn vào mọi trang `/lesson/[slug]`.
  - **Đánh dấu "đã hoàn thành"** (`lessonDone.ts`, localStorage): badge "✓ Đã hoàn thành" trên đầu bài sau khi làm quiz.
  - **Gợi ý bài học hôm nay** (`SuggestedLesson.tsx`) trên `/today`: ưu tiên bài đang học dở → bài mới mở khoá → bài chưa làm quiz. Đã verify trên Chrome.
- [ ] (Còn lại theo yêu cầu) làm dày 22 bài cũ lên 12–15 cụm; thêm bộ từ vựng & câu mới.

### Chất lượng code & tiến bộ theo thời gian (2026-06-02)
- [x] **ESLint: 23 lỗi → 0**. Sửa triệt để: `Math.random` trong render (`listening-exercises` → xáo trộn seeded theo `ex.id`), đọc ref khi render (`SprintGame` → hàm `replay()`), `prefer-const`, 4 ký tự `"` chưa escape (trang chủ), biến/`eslint-disable` thừa. 15 lỗi `react-hooks/set-state-in-effect` (escape-hatch hợp lệ: đọc localStorage sau mount, đặt cờ loading trước async, đóng menu khi đổi route) → hạ rule xuống `warn` trong `eslint.config.mjs` kèm giải thích, không refactor effect đang chạy ổn (chưa có test bảo vệ).
- [x] **Unit test (Vitest)** — mới: `npm test` / `test:watch`, `vitest.config.ts` (alias `@`). **36 test** trong `tests/`: `srs.test.ts` (FSRS — bất biến, không giá trị tuyệt đối vì bật `enable_fuzz`), `utils.test.ts` (`countSentences`, `parseJsonLoose`), `parseExercises.test.ts` (mc/truefalse/fill/ordering/transcript/keys), `lessonProgress.test.ts` (đếm tag + mở khoá động, lấy slug động từ `stages`).
- [x] **README viết lại** (trước là template `create-next-app`): mô tả, stack, setup, bảng env, thứ tự chạy migration DB, scripts, test. Bổ sung `SUPABASE_SERVICE_ROLE_KEY` + `ADMIN_PASSCODE` vào `.env.example`.
- [x] **Trang `/dashboard` dark-glass theo mẫu mới** (2026-06-02): dựng trang mẫu mới theo ảnh design người dùng gửi (dark glassmorphic, vòng tiến độ tròn). Hướng đã chốt: **indigo chủ đạo + điểm nhấn vàng gold**. Thêm token `--gold`/`--gold-soft` (light + dark) vào `globals.css`. Component tái dùng `ProgressRing.tsx` (SVG, track mờ + cung gold animate). Canvas tối cố định (hào quang tím góc) bất kể theme; thẻ lớn viền gradient indigo→fuchsia chứa vòng "Hiểu→Nói được %" + 3 mục tiêu; cột phải checklist 3 nhiệm vụ hôm nay (badge check gold khi xong); hàng dưới "Tiến độ tuần" (mini BarChart 7 ngày, gold) + "Sắp đến hạn". **Dữ liệu THẬT** qua `loadDashboard` + `loadActivityTimeline(7)` (không migration). Link "Bảng điều khiển" 🧭 vào Nav. Đã verify trình duyệt — 0 lỗi console. Lint sạch · build sạch (38 route). (Đây là bản mẫu để xem trước khi quyết định nhân rộng phong cách.)
  - **Theme-aware (sáng/tối)**: ban đầu canvas cố định tối → ở chế độ sáng thành khối đen lạc lõng. Đã chuyển toàn bộ màu hardcode sang token/`dark:`. Thêm token `--ring-track` (sáng/tối) cho track vòng tiến độ. Vòng gold + nút gold là điểm nhấn ở cả hai.
  - **Tinh chỉnh light theo phản hồi**: bản indigo/fuchsia nhạt nhìn "đục" → đổi **chế độ sáng = nền trắng sạch + thẻ liquid-glass trắng (shadow mềm + viền mảnh)**, tắt glow tím ở light (chỉ hiện ở dark), viền thẻ lớn về hairline ở light (gradient indigo→fuchsia chỉ ở dark). Chế độ tối giữ nguyên dark-glass như mẫu. Đã verify cả 2 chế độ trên trình duyệt — 0 lỗi console.
  - **Redesign theo mẫu mới (frosted + 3 vòng pastel)** (2026-06-02): theo ảnh design thứ 2 người dùng gửi. Bố cục 3 cột (`liquid-glass-card`): **Phân tích nhanh** (mini BarChart tuần + Điểm TB + gợi ý động theo chỉ số thấp nhất) · **3 vòng tiến độ pastel** (mint=Nói được mastery%, peach=Phát âm shadowAvg, lavender=Vốn từ = recognized/tổng cụm curriculum) · **Việc cần làm** (3 nhiệm vụ + nút gradient mint "Bắt đầu học"). Số liệu THẬT (`loadDashboard` + `loadActivityTimeline(7)` + `TOTAL_PHRASES` từ `lessonContent`). Canvas: sáng = frosted bạc mềm, tối = dark-glass. Vòng dùng `ProgressRing` với màu pastel. Đã verify cả 2 chế độ — 0 lỗi console. Lint sạch · build sạch.
  - **Tăng chất liquid-glass cho light** (2026-06-02, theo ảnh mẫu rõ hơn): đổi nền sáng từ xám-bạc phẳng → **gradient lavender** + thêm **3 vầng pastel mờ (mint/peach/violet) hắt sau các thẻ** (chỉ ở light, `dark:hidden`) để thẻ `liquid-glass-card` frosted bắt màu → ra chất kính thật. Viền canvas `border-white/50` ở light. Nút "Bắt đầu học" đổi gradient **mint→peach** như mẫu. Bố cục 3 cột ở desktop (≥lg), xếp dọc ở hẹp. Đã verify cả light/dark + khung 1440px — 0 lỗi console.
  - **Khớp sát mẫu (3 thẻ kính trôi nổi)** (2026-06-02): phản hồi "chưa giống" → (1) **bỏ khung viền bao ngoài**, để 3 thẻ rời trôi trên sân khấu gradient; (2) thẻ **trong hơn** — thay `liquid-glass-card` bằng `bg-white/30 + backdrop-blur-2xl + viền white/50 + shadow mềm` (dark: `bg-white/[0.05]`) để nhìn xuyên nền; (3) **so le độ cao** — grid `lg:items-center` + thẻ giữa padding lớn hơn → thẻ giữa nổi cao, 2 thẻ bên canh giữa như mẫu. Vầng pastel hắt sau to/rõ hơn. Đã verify light/dark @1366px — 0 lỗi console.
  - **G0 — Nền tảng design-system glassmorphism toàn app** (2026-06-02): hướng đã chốt **trung tính + pastel + emerald**, rollout theo phase. (1) Đưa `.glass-card` (light+dark) từ inline dashboard vào `globals.css` (utility chung). (2) **Nền toàn cục**: `body` = radial-gradient sáng (dark: radial tối); đổi 3 `fluid-blob` của layout sang **pastel mint/lavender/peach**, làm **tĩnh** (bỏ animation) + giữ `will-change/translateZ` cho GPU layer (nhẹ hơn). (3) Tạo primitive `GlassCard.tsx` dùng chung cho các phase sau. (4) Refactor `dashboard/page.tsx`: bỏ `<style>` inline + bỏ hack ẩn blob + bỏ nền radial riêng → dùng nền & `.glass-card` chung. `.glass-card` giữ đúng thông số người dùng đã tinh chỉnh (blur 40px + inset shadows). Build sạch (38 route) · lint 0 lỗi. ⚠️ *Công cụ screenshot headless bị kẹt tiến trình capture sau nhiều lần chụp trong phiên (eval/DOM vẫn OK) → chưa chụp được ảnh xác nhận; cần soi trực tiếp trên trình duyệt.* Các phase G1–G3 (migrate từng trang) chưa làm.
  - **Áp phương pháp học IPA hiệu quả vào bài** (2026-06-02): tra cứu phương pháp (shadowing; minimal pairs — nghiên cứu UBC 2019: 14h luyện → +35% nghe phân biệt; ghi âm & so sánh; học từng cụm nhỏ/ngày). Áp vào `ipa-sounds`: (1) viết lại intro/tip thành **quy trình 5 bước** (nghe 🔊 → nhại/shadowing → đọc IPA chậm → 3–5 âm/ngày → ghi âm ở mục Shadowing); (2) thêm mục **8 cặp âm dễ nhầm (minimal pairs)** cho người Việt: ship/sheep, bad/bed, think/sink, they/day, very/berry, right/light, ice/eyes, sin/sing. Tổng 52 cụm (44 âm + 8 cặp), build sạch, 36 test pass. (Cần seed lại để hiển thị cho tài khoản CMS.) Nguồn: learnenglishsounds.com (shadowing), quickpronounce.site (minimal pairs/UBC), talkpal.ai (IPA).
  - **Thêm chế độ "📝 Kiểm tra" vào `/ipa`** (2026-06-04): sau khi gộp IPA, bài quiz cuối bài (`LessonQuiz`) bị mất → bổ sung mode "Kiểm tra" (tab thứ 4) trên `/ipa`. Bài kiểm tra **hữu hạn 12 câu, có điểm**, **trộn 2 dạng**: (A) **nhận âm** — cho ký hiệu /X/ + mô tả → chọn từ chứa âm đó (4 lựa chọn); (B) **phân biệt cặp âm giống nhau** — nghe 1 từ trong minimal pair (tự phát qua effect + nút 🔊 nghe lại) → chọn đúng từ (ship/sheep…). 6 câu mỗi loại, shuffle. Chọn xong chấm đúng/sai + phát từ đúng + câu tiếp; cuối có điểm % + làm lại. Verify trình duyệt: dạng A (/eɪ→day đúng), dạng B (prompt "Nghe và chọn từ bạn nghe được"), chạy trọn 12 câu ra kết quả, 0 lỗi console. Lint/build sạch. (`QuizMode` union type sound|pair trong `src/app/ipa/page.tsx`.)
  - **Gộp IPA về 1 trang `/ipa`** (2026-06-04): bài lesson `ipa-sounds` (lưới gọn) bị trùng cả nội dung lẫn giao diện với trang `/ipa` tương tác → quyết định gộp. (1) Lộ trình: thẻ "Âm IPA cơ bản" trỏ thẳng `/ipa` (thêm `TOOL_HREF` trong RoadmapPath, tổng quát hóa từ pattern ai-roleplay→/roleplay). (2) `computeStatusesView` (lessonsView.ts): thêm `TOOL_SLUGS={ipa-sounds, ai-roleplay}` — luôn "available", KHÔNG chặn chuỗi mở khóa → bài kế (greetings) mở bình thường. (3) `/lesson/ipa-sounds` redirect sang `/ipa` (useRouter.replace) + đã revert/xóa code special-case lưới gọn vừa thêm (helpers/import dọn sạch). Verify: thẻ lộ trình href=/ipa, /lesson/ipa-sounds→/ipa, 0 lỗi console, lint/build sạch, 36 test pass. Giờ IPA chỉ còn MỘT trải nghiệm duy nhất (/ipa: bảng âm + nghe-nhại + phân biệt + video).
  - **Tối ưu UI/UX bài `ipa-sounds`** (2026-06-04): trước render 56 thẻ to xếp dọc → cuộn dài, khó dùng. Special-case slug `ipa-sounds` trong `/lesson/[slug]/page.tsx`: **lưới 2 cột compact (sm:grid-cols-2)**, chia 4 nhóm (Phụ âm/Nguyên âm đơn/Nguyên âm đôi/Cặp dễ nhầm) bằng map symbol→type từ `IPA_SOUNDS`; mỗi âm 1 dòng nhỏ: 🔊 (đọc đúng *từ* mẫu thay vì đọc cả ký hiệu) + ký hiệu/từ + mô tả + nút lưu nhanh (+/✓). Các bài khác giữ layout cũ. Verify trình duyệt (1 cột mobile / 2 cột desktop), 0 lỗi console, lint/build sạch. (Nhóm "Nguyên âm đơn" sẽ chuẩn sau khi seed lại bản 56 âm — hiện DB còn bản cũ.)
  - **/today nền trong suốt như Dashboard (bỏ nền màu)** (2026-06-04): Dashboard đã được rework sang nền TRANG TRONG SUỐT (không gradient/glow) + thẻ class `.glass-card` (white/0.08 + blur 40px + inset highlight). /today trước còn gradient lavender + glow → "nhiều màu nền". → Bỏ hẳn gradient/glow ở Stage (thành khung trong suốt), thẻ chuyển sang dùng chung `.glass-card`. Giờ màu chỉ ở vòng/chip/nút accent; nền sạch trong y hệt Dashboard. Verify light @ /today — 0 lỗi console, lint sạch.
  - **/today dùng "sân khấu" liquid-glass giống Dashboard** (2026-06-04): bản reskin trước để thẻ trắng đục trên nền phẳng (nhìn "lì"). → TodayDashboard giờ render component `Stage` (nền gradient lavender + 3 vầng pastel hắt sau ở light, glow tím ở dark — y hệt /dashboard) bọc toàn bộ thẻ; thẻ đổi sang `bg-white/15 + backdrop-blur-2xl` (trong, khúc xạ màu). Verify light @ /today: nền lavender + glow + thẻ kính trong đúng chất Dashboard, 0 lỗi console, lint/build sạch.
  - **Reskin /today & /statistics theo liquid-glass tối giản** (2026-06-04): đồng bộ phong cách với Bảng điều khiển, giữ VAI TRÒ RIÊNG. `/today` (TodayDashboard): bỏ thẻ gradient nặng + lưới stats rời → 1 thẻ tổng quan frosted (ProgressRing "Nói được" mint + 4 chip số liệu) + gợi ý bài học + **3 việc cần làm** pastel (mint/amber/violet) — hướng HÀNH ĐỘNG. `/statistics`: thêm `GLASS` (frosted nhẹ white/55 + dark variant), làm gọn StatBlock/ActivityRow, đổi 4 card wrapper từ liquid-glass-card nặng → GLASS; giữ nguyên biểu đồ hoạt động 7/14/30 ngày + lượt ôn/phát âm — PHÂN TÍCH SÂU. Cả 2 theme-aware. Verify: /today dark + /statistics light đều đẹp, 0 lỗi console, lint/build sạch.
  - **Nút sang CMS (admin/editor)** (2026-06-04): thêm hook `useCmsRole()` (độc lập, không cần AdminAuthProvider — dùng session Supabase + /api/admin/ping) trong `adminAuth.tsx`. Hiện nút **"🛠️ Trang quản trị"** (admin) / **"Quản trị nội dung"** (editor) trỏ `/admin/lessons` ở (1) trang chủ cạnh CTA, và (2) **thanh Nav trái cố định** (khu footer, trên thẻ tài khoản) — vào được từ mọi trang. Ẩn với user thường/ẩn danh. Verify: ẩn danh → ẩn; build/lint sạch.
  - **Quản lý tài khoản & phân quyền trong CMS** (2026-06-04): thêm RBAC 2 cấp. Migration `db/migrate_roles.sql` thêm cột `profiles.role` ('admin'|'editor'). `supabaseAdmin.ts`: `getRequestRole` (email ∈ ADMIN_EMAILS → admin cố định; ngược lại đọc profiles.role) + `checkAdmin` (admin) + `checkCms` (admin|editor). Route nội dung (lessons/listening/…) dùng `checkCms`; seed-lessons + route tài khoản dùng `checkAdmin`. Route mới `/api/admin/users` (GET danh sách + PATCH đổi role, admin-only; email cố định không sửa được qua UI). Trang mới `/admin/users` (avatar, badge vai trò, select Người dùng/Editor/Admin, tìm kiếm). Menu "👥 Tài khoản" chỉ admin thấy; `adminAuth` thêm `role`/`isAdmin` (ping trả role). Verify: users API GET/PATCH→401 với non-admin, /admin/users ẩn danh→gate. Build/lint sạch. ⚠️ Cần chạy `db/migrate_roles.sql` để dùng (admin cố định qua ADMIN_EMAILS vẫn vào CMS được trước khi migrate; danh sách tài khoản cần cột role).
  - **Admin auth đồng bộ với tài khoản (bỏ passcode)** (2026-06-04): trước admin = `ADMIN_PASSCODE` riêng. Giờ **admin = user đăng nhập có email trong `ADMIN_EMAILS`** (env, phân tách dấu phẩy). `checkAdmin(req)` → async: lấy token Supabase từ header `x-admin-key` (giữ tương thích client) hoặc `Authorization: Bearer` → `getUser` → email phải thuộc ADMIN_EMAILS. `adminConfigured()` = chỉ cần service role. Client `adminAuth.tsx` dùng `supabase.auth.getSession()` (token + onAuthStateChange refresh) thay passcode; bỏ login/logout/error, thêm `email`. Admin layout: màn nhập mật mã → màn "đăng nhập tài khoản admin" / "tài khoản X không có quyền"; Đăng xuất = `supabase.auth.signOut()`. Routes: helper `guard()` + handler → async/await checkAdmin (8 route). `.env`: `ADMIN_PASSCODE`→`ADMIN_EMAILS` (đã set `hoantn@vtvlive.vn` trong .env.local). Verify: ping token giả→401, /admin ẩn danh→màn đăng nhập (hết ô passcode), build/lint sạch. ⚠️ Cần restart dev để nạp ADMIN_EMAILS; đăng nhập app bằng email admin để vào CMS.
  - **Seed CMS xong + verify** (2026-06-04): người dùng bấm "↻ Seed lại" → DB cập nhật. Verify lộ trình (loadStages từ DB, tài khoản đăng nhập): A1(12) gồm present-continuous; A2(11) gồm past-simple + describe-compare (đã chuyển cấp đúng); B1(11) đủ 5 bài ngữ pháp mới; B2(6) đủ 3 bài mới. Bài /lesson/relative-clauses render nội dung OK. 0 lỗi console. → Toàn bộ plan IPA/lộ trình HOÀN TẤT.
  - **Soạn nội dung 3 bài ngữ pháp B2** (2026-06-04): hoàn thiện 3 placeholder cuối lộ trình — `narrative-tenses` (thì kể chuyện: QK đơn/tiếp diễn/hoàn thành), `third-conditional` (điều kiện loại 3 & hỗn hợp), `relative-clauses` (mệnh đề quan hệ who/which/that/whose/where). Mỗi bài 9 cụm + câu mẫu + IPA, trong `lessons.ts`. `lint:cefr` không cảnh báo vượt cấp (B2-clean), build sạch, 36 test pass. Verify trình duyệt: `/lesson/third-conditional` render đúng (h1 + B2 + các cụm). ⚠️ Gặp lại lỗi `.next` hỏng (chạy `npm run build` xen kẽ `next dev`) làm mọi `/lesson/*` trả 404 → xóa `.next` + restart dev là hết. → Lộ trình A1→B2 giờ đã có nội dung cho mọi bài ngữ pháp.
  - **Nhúng video shadow BBC cho đủ 44 âm** (2026-06-04): người dùng cấp playlist **BBC Learning English "The Sounds of English"** (PLD6B222E02447DC07). Trích 45 video (44 âm + 1 intro) qua `ytInitialData`, map symbol→videoId thành `IPA_VIDEO` trong `data/ipa.ts`. Modal mỗi âm ở `/ipa` nhúng **iframe youtube-nocookie** video BBC tương ứng (khẩu hình để shadow). Verify: /θ/ → embed `b4Aj3k65HSo` (video "Voiceless consonant /θ/"), 0 lỗi console, lint/build sạch. → Task "video shadow theo âm" HOÀN THÀNH.
  - **Gộp 1 nguồn dữ liệu IPA + video shadow theo âm** (2026-06-02): trang `/ipa` (component user dựng) và bài `ipa-sounds` trước lưu 44 âm ở 2 nơi (đã lệch 45 vs 44). → Gộp về **một nguồn `src/data/ipa.ts`** (44 âm + 12 cặp): `lessons.ts` import `IPA_SOUNDS`/`MINIMAL_PAIRS` và sinh `phrases` qua hàm `ipaSoundPhrases()` (56 cụm) — sửa 1 nơi, cả hai cập nhật. Thêm field tùy chọn `videoId` vào `IpaSound`; modal mỗi âm ở `/ipa` **nhúng iframe youtube-nocookie nếu có `videoId`**, chưa có thì hiện nút **🎬 mở video khẩu hình** (tìm kiếm YouTube lọc "BBC Sounds of English /âm/" — luôn sống, không chết link). Verify trình duyệt: modal /θ/ hiện nút video + link encode đúng, 0 lỗi console. Lint/build sạch, 36 test pass. (Còn lại: điền `videoId` thật để nhúng embed — chờ tra playlist khi web search hết giới hạn phiên.)
  - **Thêm nút "Seed lại" vào Admin CMS** (2026-06-02): trước chỉ có API `/api/admin/seed-lessons`, không có nút bấm. Thêm nút **"↻ Seed lại"** ở header trang `/admin/lessons` (cạnh "+ Bài mới") — gọi POST seed với header `x-admin-key`, có `confirm()` cảnh báo ghi đè phrases (giữ audio), xong tự reload danh sách. Lint sạch, build sạch.
  - **Cập nhật bài IPA đủ 44 âm** (2026-06-02): bài `ipa-sounds` trước chỉ có 7 cặp âm khó → viết lại thành **trọn bộ 44 âm chuẩn IPA** (24 phụ âm + 12 nguyên âm đơn + 8 nguyên âm đôi), mỗi âm có từ ví dụ, hướng dẫn khẩu hình tiếng Việt, IPA, câu mẫu; ghi chú riêng âm khó với người Việt (/θ/ /ð/ /v/ /z/ /ʒ/ /r/) và cặp ngắn–dài. Đếm = 44 cụm, build sạch, 36 test pass. (Lưu ý: gặp lỗi `.next/dev/types/routes.d.ts` hỏng do restart dev nhiều lần → xóa `.next` build lại là hết.) Hiển thị cho user CMS cần chạy lại seed (ipa-sounds đã có trong DB bản cũ).
  - **Soạn nội dung 5 bài ngữ pháp B1** (2026-06-02): A1/A2 và chủ đề B1 (restaurant/travel/opinions) đã có nội dung sẵn → phần trống trong A1→B1 là 5 bài ngữ pháp B1 vừa thêm. Đã soạn nội dung gốc (lối "cụm + câu mẫu", 9 cụm/bài, kèm IPA) trong `lessons.ts` cho: present-perfect-continuous, second-conditional, modals-deduction, passive-simple, reported-speech. `npm run lint:cefr` không cảnh báo vượt cấp cho 5 bài này (B1-clean). DOM xác minh trang `/lesson/second-conditional` render đủ cụm; 36 test pass, build sạch. Còn lại (B2): narrative-tenses, third-conditional, relative-clauses chưa soạn (ngoài phạm vi A1→B1).
  - **Chuẩn hóa lộ trình theo CEFR A1→B2** (2026-06-02): tra cứu chuẩn CEFR (grammar syllabus + can-do theo cấp) rồi viết lại `curriculum.ts`. (1) Khung 4 giai đoạn chuẩn **A1 Khởi đầu / A2 Sơ cấp / B1 Trung cấp / B2 Trung cao cấp** (trước: A1 Movers / A2 Flyers / B1 / B1+) — đồng thời **sửa mâu thuẫn** stage 4 (curriculum B1+ vs placement.ts đã là B2). (2) Mục tiêu mỗi giai đoạn viết lại theo can-do + trọng tâm ngữ pháp đúng cấp. (3) **Sửa bài đặt sai cấp**: past-simple & describe-compare (so sánh) A1→A2; present-continuous A2→A1 (đúng chuẩn CEFR adult). (4) **Lấp khoảng trống ngữ pháp B1** (trước chỉ có chủ đề tình huống): thêm 5 mốc — điều kiện loại 2, modal suy đoán, bị động đơn, tường thuật, HT hoàn thành tiếp diễn. (5) Thêm giai đoạn **B2** với narrative tenses, điều kiện loại 3/hỗn hợp, mệnh đề quan hệ + fluency (small-talk, think-in-english, ai-roleplay). Bài chưa soạn hiện "khóa" trên lộ trình (mốc chuẩn để soạn dần). Metadata giai đoạn áp dụng ngay mọi nơi; bài đổi cấp cần chạy lại POST /api/admin/seed-lessons để cập nhật CMS. DOM xác minh 4 giai đoạn + 8 bài ngữ pháp mới hiển thị, 0 lỗi console, lint/build sạch. Nguồn: examenglish.com/CEFR, tracktest.eu grammar, Council of Europe can-do.
  - **Tối ưu trang chủ `/` thành trang Lộ trình học** (2026-06-02): trước là trang marketing nhiều section + dữ liệu giả (mockup game "break a leg/940", flashcard "once in a blue moon/88%/124 cụm", vòng dashboard giả 75/60/90 trùng trang Dashboard thật, playground demo, stats 180+/22+). Người dùng phản hồi "nhiều thứ không liên quan". → Viết lại: **header gọn** (chào theo tên + 2 nút tắt "Học hôm nay" /today · "Bảng điều khiển" /dashboard + chip streak) + **`RoadmapPath` làm trọng tâm** (4 giai đoạn A1→giao tiếp) + footer gọn. Bỏ hết marketing/dữ liệu giả; bỏ import `CoreLoopDemo`/`useState`. DOM xác minh: h1 đúng, 0 section giả còn sót, roadmap render đủ stage, 0 lỗi console (screenshot timeout do RoadmapPath có animation SVG liên tục — giới hạn tool). Lint sạch · build sạch.
  - **Thay hero "voice waves" ở trang chủ** (2026-06-02): ảnh `hero_wave_3d.png` (3D tối) bị crop lệch trong khung vuông `object-cover` + chỏi tông kính sáng → **thay bằng equalizer dựng CSS** (15 thanh `.wave-bar` đổi màu primary→accent→pink) trên nền gradient + vầng pastel mềm, giữ badge "Hội thoại AI đang trực tuyến". Bỏ import `next/image` thừa ở `page.tsx`. (PNG cũ giờ là asset chết, có thể xoá.) Xác minh qua DOM: 15 thanh căn giữa, không tràn thẻ, 0 lỗi console (tool screenshot timeout do trang chủ nhiều animation blur — không phải lỗi).
    - **Sửa badge "Hội thoại AI" bị thô**: nguyên nhân gốc — class `liquid-glass-card` đặt `position: relative` **đè** class `absolute` của badge → badge rớt vào luồng, thành hộp hẹp cạnh sóng, chữ chen 3 dòng. Fix: badge dùng glass tự đặt (`bg-surface/80 + backdrop-blur-xl + border`, bỏ `liquid-glass-card`) để giữ `absolute`; rút gọn tiêu đề "Hội thoại AI · Trực tuyến" + `truncate` mỗi dòng; chấm "ping" mềm. DOM xác minh: badge absolute, full-width (380/414px), neo đáy, subtitle 1 dòng.
  - **Trong hơn "y hệt design"** (2026-06-02): giảm độ đục thẻ `bg-white/30 → /15` (nhìn xuyên thấy nền pastel), viền `white/60`, nền sáng airier (`#f6f5fd…`), thêm phản chiếu sàn mờ phía dưới (light). Lưu ý hiệu năng: `backdrop-blur-3xl` quá nặng (treo cả screenshot headless) → dùng `backdrop-blur-2xl` (vẫn frosty, nhẹ GPU). Đã verify light/dark @1366px — 0 lỗi console.
- [x] **Polish & sửa warning thật** (2026-06-02): (a) **Audio-call** — `score` được cộng nhưng chưa bao giờ hiển thị → thêm "ĐIỂM" nổi bật ở màn kết quả (đã verify e2e: chơi tới hết mạng → hiện điểm). (b) **Trang chủ** ảnh hero `<img>` → `next/image` (tối ưu LCP + srcset responsive, `priority`). (c) **Admin listening-exercises** `gridRegion` bọc `useCallback([gm])` để effect vẽ lưới có dep đầy đủ. ⇒ warning 18 → **15** (15 còn lại đều là `set-state-in-effect` đã chủ ý để `warn`).
- [x] **Biểu đồ tiến bộ theo thời gian** ở `/statistics`: thẻ "Hoạt động N ngày" — nút đổi khoảng **7/14/30 ngày** (tải sẵn 30 ngày, cắt ở client) + nút đổi metric **Lượt ôn** (BarChart: số lượt ôn/ngày, tooltip đánh dấu 📓 ngày có nhật ký) ↔ **Phát âm** (LineChart: điểm Shadowing TB/ngày, thang 0–100). 2 chip tổng hợp đổi theo metric (Ngày học / Phát âm TB), empty state riêng cho từng metric. Dữ liệu THẬT từ `review_logs`/`journal_entries`/`shadowing_attempts` qua `loadActivityTimeline` (`statsRepo.ts`) — **không cần migration**, đều scoped theo user qua RLS. Đã verify trên trình duyệt (light, đổi 30 ngày, đổi tab Phát âm, mobile) — 0 lỗi console. Lint sạch · build sạch · 36/36 test pass.

## Biến môi trường (`.env.local`)
Xem mẫu đầy đủ ở `.env.example`. Tóm tắt:
```
NEXT_PUBLIC_SUPABASE_URL=...        # đã có
NEXT_PUBLIC_SUPABASE_ANON_KEY=...   # đã có
GEMINI_API_KEY=                     # cần thêm
AZURE_SPEECH_KEY=                   # cần thêm
AZURE_SPEECH_REGION=southeastasia   # cần thêm
```
> Mọi key AI chỉ ở server (không có tiền tố `NEXT_PUBLIC_`). Azure dùng token ngắn hạn, key gốc không xuống client.

## Trạng thái build
✅ `next build` sạch — 13 route (gồm 3 API route: journal-feedback, roleplay, speech-token).
- [x] **Rà & sửa nút/modal lỗi hiển thị** (2026-06-04): nút disabled dùng `liquid-glass-btn` (nền màu) + ép `disabled:!text-muted/40` → chữ mờ tịt trên nền xanh (nền không bị `!bg-black/5` ghi đè). Sửa sang `disabled:opacity-55` ở **Nhật ký** (nút chấm điểm), **Chép chính tả** (Kiểm tra/Tiếp theo), **bài học** (lưu & chấm). Rà toàn bộ: chỉ 2 modal có `fixed inset-0` (IPA, DictionaryModal) — đã portal + khoá cuộn; overlay menu mobile trong Nav không bị nhốt. App typecheck sạch. (Audit DB: mọi migration đã chạy — error_notes/listening_exercises(26)/word_examples(141)/listen_videos(30)/push(1).)
